<x-layouts.mobile title="Profil Siswa" backUrl="{{ route('attendance.my-class.index') }}">
    <div class="space-y-4 p-4">
        <section class="solid-panel rounded-[24px] p-5 text-left"><p class="text-[10px] font-bold uppercase tracking-wider theme-text-muted">{{ $assignment->schoolClass->name }}</p><h1 class="mt-1 text-lg font-black theme-text-main">{{ $student->nama_lengkap }}</h1><div class="mt-3 grid grid-cols-2 gap-2 text-xs"><div><span class="theme-text-muted">NISN</span><p class="font-bold theme-text-main">{{ $student->nisn ?? '-' }}</p></div><div><span class="theme-text-muted">Status</span><p class="font-bold theme-text-main">{{ $student->status }}</p></div><div><span class="theme-text-muted">Jenis kelamin</span><p class="font-bold theme-text-main">{{ $student->jenis_kelamin === 'L' ? 'Laki-laki' : ($student->jenis_kelamin === 'P' ? 'Perempuan' : '-') }}</p></div><div><span class="theme-text-muted">Orang tua/wali</span><p class="font-bold theme-text-main">{{ $student->nama_wali ?: ($student->nama_ayah_kandung ?: $student->nama_ibu_kandung) ?: '-' }}</p></div></div></section>
        <section><h2 class="mb-3 text-xs font-black uppercase tracking-wider theme-text-main">Ringkasan Pelanggaran</h2><div class="space-y-2">@forelse($violations as $record)<a href="{{ route('attendance.my-class.violations.show', $record) }}" class="solid-panel block rounded-2xl p-4 text-left"><div class="flex items-start justify-between gap-3"><span class="font-bold theme-text-main">{{ $record->category?->name ?? $record->custom_topic ?? 'Pelanggaran' }}</span><span class="text-[10px] theme-text-muted">{{ $record->occurred_at->format('d M Y') }}</span></div><p class="mt-1 line-clamp-2 text-xs theme-text-muted">{{ $record->chronology }}</p></a>@empty<div class="solid-panel rounded-2xl p-6 text-center"><p class="font-bold theme-text-main">Tidak ada pelanggaran</p><p class="mt-1 text-xs theme-text-muted">Belum ada catatan pelanggaran aktif untuk siswa ini.</p></div>@endforelse</div></section>
    </div>
</x-layouts.mobile>
