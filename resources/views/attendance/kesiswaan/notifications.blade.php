<x-layouts.app><div class="space-y-4"><div class="flex justify-between"><h1 class="text-xl font-bold">Notifikasi Kesiswaan</h1><form method="POST" action="{{route('attendance.kesiswaan.notifications.read-all')}}">@csrf @method('PATCH')<button class="underline">Tandai semua dibaca</button></form></div>@forelse($notifications as $notification)<a class="block rounded border p-3 {{$notification->read_at?'opacity-60':'font-bold'}}" href="{{route('attendance.kesiswaan.notifications.show',$notification)}}">{{$notification->data['student_name'] ?? 'Rujukan siswa'}} · {{$notification->data['status'] ?? 'baru'}}<div class="text-xs">{{$notification->created_at->diffForHumans()}}</div></a>@empty<p>Belum ada notifikasi.</p>@endforelse {{$notifications->links()}}</div></x-layouts.app>
