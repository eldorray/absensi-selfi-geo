<x-layouts.mobile title="Catatan BK" backUrl="{{ route('attendance.dashboard') }}" activeTab="bk"><div class="space-y-4"><div class="flex items-center justify-between"><div><h1 class="font-display text-xl font-black theme-text-main">Catatan BK</h1><p class="text-xs theme-text-muted">Catatan bimbingan dan konseling Anda.</p></div><a href="{{ route('attendance.bk.create') }}" class="theme-btn-submit rounded-full px-4 py-3 text-xs font-bold">Tambah</a></div><div class="space-y-3">@forelse($records as $record)<a href="{{ route('attendance.bk.show',$record) }}" class="glass-card theme-border block rounded-[22px] p-4"><p class="font-bold theme-text-main">{{ $record->student->nama_lengkap }}</p><p class="text-xs theme-text-muted">{{ $record->occurred_at->format('d M Y H:i') }} · {{ $record->category?->name ?? $record->custom_topic }} · {{ $record->status }}</p></a>@empty<div class="glass-card theme-border rounded-[22px] p-6 text-center"><p class="font-bold theme-text-main">Belum ada catatan</p><p class="mt-1 text-xs theme-text-muted">Buat catatan pertama untuk siswa yang Anda tangani.</p></div>@endforelse</div>{{ $records->links() }}</div></x-layouts.mobile>
